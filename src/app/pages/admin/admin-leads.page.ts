import { Component, inject, signal } from '@angular/core';
import { BaseTableComponent, TableAction } from '../../base/base-table/base-table.component';
import { BaseDialogComponent } from '../../base/base-dialog/base-dialog.component';
import { BaseDatePickerComponent } from '../../base/base-date-picker/base-date-picker.component';
import { BaseToastService } from '../../base/base-toast/base-toast.service';
import { leads } from '../../shared/mock-data';
@Component({selector:'app-admin-leads-page',standalone:true,imports:[BaseTableComponent,BaseDialogComponent,BaseDatePickerComponent],template:`<section class="screen-stack admin-dashboard"><article class="hero-card"><small>Admin Dashboard</small><h2>System Leads Management</h2><p>Review, filter, assign, and maintain system leads.</p></article><app-base-table [columns]="columns" [rows]="leads" [filters]="filters" [customActions]="actions" (add)="open('Add Lead')" (edit)="open('Edit Lead')" (delete)="remove()" /></section><app-base-dialog [open]="!!dialog()" [title]="dialog()" (closed)="dialog.set('')" (confirm)="save()"><div class="form-grid"><input class="control" placeholder="Lead name"/><input class="control" placeholder="Phone"/><select class="control"><option>New</option><option>Called</option></select><app-base-date-picker label="Follow-up date" /></div></app-base-dialog>`})
export class AdminLeadsPage{private toast=inject(BaseToastService);leads=leads;dialog=signal('');columns=[{key:'name',label:'Name'},{key:'phone',label:'Phone'},{key:'status',label:'Status'},{key:'assigned',label:'Consultant'}];filters=[{key:'status',label:'Status',type:'select' as const,options:['New','Called']},{key:'assigned',label:'Consultant',type:'text' as const}];actions:TableAction[]=[{icon:'☎️',label:'Call',onClick:()=>this.toast.info('Mock call action triggered')},{icon:'👤',label:'Assign Lead',onClick:()=>this.open('Assign Lead')}];open(t:string){this.dialog.set(t)}save(){this.toast.success('Lead action saved in mock UI');this.dialog.set('')}remove(){this.toast.warning('Lead removed in mock UI')}}
