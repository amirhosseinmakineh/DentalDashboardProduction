import { Component, inject, signal } from '@angular/core';
import { BaseTableComponent, TableAction } from '../../base/base-table/base-table.component';
import { BaseDialogComponent } from '../../base/base-dialog/base-dialog.component';
import { BaseDatePickerComponent } from '../../base/base-date-picker/base-date-picker.component';
import { BaseToastService } from '../../base/base-toast/base-toast.service';
import { leads } from '../../shared/mock-data';
@Component({selector:'app-consultant-leads-page',standalone:true,imports:[BaseTableComponent,BaseDialogComponent,BaseDatePickerComponent],template:`<section class="screen-stack consultant-dashboard"><article class="hero-card"><small>Consultant Dashboard</small><h2>My Leads</h2><p>Call users, add notes, create profiles, and reserve appointments.</p></article><app-base-table [columns]="columns" [rows]="leads" [filters]="filters" [customActions]="actions" (add)="open('Add Lead')" /></section><app-base-dialog [open]="!!dialog()" [title]="dialog()" (closed)="dialog.set('')" (confirm)="save()"><div class="form-grid"><textarea class="control" placeholder="Note or lead details"></textarea><app-base-date-picker label="Follow-up date" /></div></app-base-dialog>`})
export class ConsultantLeadsPage{private toast=inject(BaseToastService);leads=leads;dialog=signal('');columns=[{key:'name',label:'Name'},{key:'phone',label:'Phone'},{key:'status',label:'Status'},{key:'description',label:'Description'}];filters=[{key:'status',label:'Status',type:'select' as const,options:['New','Called']},{key:'description',label:'Treatment',type:'text' as const}];actions:TableAction[]=[{icon:'☎️',label:'Call user',onClick:()=>this.open('Call user')},{icon:'📝',label:'Add note',onClick:()=>this.open('Add note')},{icon:'👤',label:'Create profile',onClick:()=>this.open('Create profile')},{icon:'📅',label:'Reserve appointment',onClick:()=>this.open('Reserve appointment')}];open(t:string){this.dialog.set(t)}save(){this.toast.success('Lead action saved in mock UI');this.dialog.set('')}}
