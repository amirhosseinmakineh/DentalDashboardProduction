/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { BaseTableComponentDirective } from './baseTableComponent.directive';

describe('Directive: BaseTableComponent', () => {
  it('should create an instance', () => {
    const directive = new BaseTableComponentDirective();
    expect(directive).toBeTruthy();
  });
});
