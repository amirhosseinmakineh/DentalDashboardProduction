import { Component, inject, signal } from '@angular/core';
import { BaseTableComponent } from '../../base/base-table/base-table.component';
import { BaseDialogComponent } from '../../base/base-dialog/base-dialog.component';
import { BaseDatePickerComponent } from '../../base/base-date-picker/base-date-picker.component';
import { BaseToastService } from '../../base/base-toast/base-toast.service';
import { consultants } from '../../shared/mock-data';
@Component({selector:'app-admin-consultants-page',standalone:true,imports:[BaseTableComponent,BaseDialogComponent,BaseDatePickerComponent],template:`<section class="screen-stack admin-dashboard"><article class="hero-card"><small>داشبورد مدیر</small><h2>مدیریت مشاوران</h2><p>ساخت، ویرایش و فیلتر حساب و تخصص مشاوران.</p></article><app-base-table [columns]="columns" [rows]="consultants" [filters]="filters" (add)="open('افزودن مشاور')" (edit)="open('ویرایش مشاور')" (delete)="remove()" /></section><app-base-dialog [open]="!!dialog()" [title]="dialog()" [confirmLabel]="dialog()==='ویرایش مشاور' ? 'ذخیره تغییرات مشاور' : 'ایجاد مشاور'" (closed)="dialog.set('')" (confirm)="save()"><div class="form-grid"><input class="control" placeholder="نام و نام خانوادگی مشاور"/><input class="control" placeholder="شماره موبایل مشاور"/><input class="control" placeholder="تخصص اصلی"/><select class="control"><option>آنلاین</option><option>حاضر</option><option>آفلاین</option></select><app-base-date-picker label="تاریخ شروع همکاری" /></div></app-base-dialog>`})
export class AdminConsultantsPage{private toast=inject(BaseToastService);consultants=consultants;dialog=signal('');columns=[{key:'name',label:'نام'},{key:'phone',label:'موبایل'},{key:'specialty',label:'تخصص'},{key:'status',label:'وضعیت'}];filters=[{key:'status',label:'وضعیت',type:'select' as const,options:['آنلاین','حاضر']},{key:'specialty',label:'تخصص',type:'text' as const}];open(t:string){this.dialog.set(t)}save(){this.toast.success(`${this.dialog()} با موفقیت ثبت شد`);this.dialog.set('')}remove(){this.toast.warning('مشاور در رابط نمایشی حذف شد')}}
