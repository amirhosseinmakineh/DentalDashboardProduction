import { Component } from '@angular/core';
import { BaseTableComponent } from '../../base/base-table/base-table.component';
import { leads, LeadStatus } from '../../shared/mock-data';
@Component({selector:'app-admin-leads-page',standalone:true,imports:[BaseTableComponent],template:`<section class="screen-stack admin-dashboard"><article class="hero-card"><small>داشبورد مدیر</small><h2>مشاهده لیدهای سیستم</h2><p>ادمین فقط لیدهای سیستم، وضعیت هر لید و مشاور تخصیص‌داده‌شده را مشاهده می‌کند؛ تماس، افزودن، ویرایش، حذف و تخصیص از این بخش حذف شده است.</p></article><app-base-table [columns]="columns" [rows]="leads" [filters]="filters" [showAdd]="false" [showEdit]="false" [showDelete]="false" /></section>`})
export class AdminLeadsPage{leads=leads;columns=[{key:'name',label:'نام لید'},{key:'phone',label:'تلفن'},{key:'status',label:'وضعیت لید'},{key:'assigned',label:'مشاور تخصیص‌داده‌شده'}];filters=[{key:'status',label:'وضعیت',type:'select' as const,options:[LeadStatus.New,LeadStatus.Called]},{key:'assigned',label:'مشاور تخصیص‌داده‌شده',type:'text' as const}]}
