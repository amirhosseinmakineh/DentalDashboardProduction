export enum AttendanceStatus {
  Present = 1,      // حاضر
  Absent = 2,       // غایب
  Leave = 3,        // مرخصی
  SickLeave = 4,    // مرخصی استعلاجی
  Late = 5,         // تأخیر
  Mission = 6       // مأموریت
}
