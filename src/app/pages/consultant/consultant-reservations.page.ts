import { Component, inject, signal } from '@angular/core';
import { BaseTableComponent } from '../../base/base-table/base-table.component';
import { BaseDialogComponent } from '../../base/base-dialog/base-dialog.component';
import { BaseDatePickerComponent } from '../../base/base-date-picker/base-date-picker.component';
import { BaseToastService } from '../../base/base-toast/base-toast.service';
import { reservations } from '../../shared/mock-data';
@Component({selector:'app-consultant-reservations-page',standalone:true,imports:[BaseTableComponent,BaseDialogComponent,BaseDatePickerComponent],template:`<section class="screen-stack consultant-dashboard"><article class="hero-card"><small>Consultant Dashboard</small><h2>Reservations</h2><p>Create and track mock reservations.</p></article><app-base-table [columns]="columns" [rows]="reservations" [filters]="filters" (add)="open('Create Reservation')" (edit)="open('Edit Reservation')" /></section><app-base-dialog [open]="!!dialog()" [title]="dialog()" (closed)="dialog.set('')" (confirm)="save()"><div class="form-grid"><input class="control" placeholder="Patient"/><app-base-date-picker label="Reservation date"/><select class="control"><option>Confirmed</option><option>Pending</option></select></div></app-base-dialog>`})
export class ConsultantReservationsPage{private toast=inject(BaseToastService);reservations=reservations;dialog=signal('');columns=[{key:'date',label:'Date'},{key:'time',label:'Time'},{key:'consultant',label:'Consultant'},{key:'status',label:'Status'}];filters=[{key:'status',label:'Status',type:'select' as const,options:['Confirmed','Pending']},{key:'date',label:'Date',type:'date' as const}];open(t:string){this.dialog.set(t)}save(){this.toast.success('Reservation action saved in mock UI');this.dialog.set('')}}
