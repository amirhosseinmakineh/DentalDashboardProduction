import { Component } from '@angular/core';

@Component({
  selector: 'app-landing',
  standalone: true,
  template: `<main class="empty-landing" aria-label="ورود و عضویت"></main>`
})
export class LandingComponent {}
